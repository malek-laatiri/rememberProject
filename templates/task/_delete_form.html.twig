<script src="https://cdn.jsdelivr.net/npm/sweetalert2@8"></script>

<form method="post" action="{{ path('admin_task_delete', {'id': idt}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ i.task.id) }}">
    <button class="btn" style="color:#5b91ee;"><i class="fas fa-trash-alt"></i></button>
</form>

{% extends 'baseFront.html.twig' %}

{% block title %}edit task{% endblock %}

{% block body %}

  <div class="col" s >  {{ include('front/_form.html.twig', {'button_label': 'Update'}) }}</div>
    <div class="col"> {{ include('front/_delete_form.html.twig') }}</div>


   <div class="col"> <a href="{{ path('user_index') }}">{% trans %}back to list{% endtrans %}</a></div>
    </div>

{% endblock %}
